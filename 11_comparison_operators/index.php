<?php
  $a = 500;
  $b = 500;

  // var_dump($a == $b); // เท่ากันไหม
  // var_dump($a === $b); // เช็กว่าเหมือนกันไหม
  // var_dump($a !== $b); // เช็กว่าไม่เหมือนกันไหม
  // var_dump($a != $b); // เช็กว่าไม่เท่ากันไหม

  // var_dump($a > $b);
  // var_dump($a >= $b);
  // var_dump($a < $b);
  // var_dump($a <= $b);

  /*
    ตัวเลข
    <=>

    a, b
    a < b = -1
    a = b = 0
    a> b = 1
  */
  var_dump($a <=> $b);


?> 

<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>ตัวดำเนินการเปรียบเทียบ</title>
</head>

<body>

</body>

</html>